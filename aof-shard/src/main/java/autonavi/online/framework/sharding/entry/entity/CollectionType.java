package autonavi.online.framework.sharding.entry.entity;

/**
 * 返回的集合类型
 * 
 * @author jia.miao
 * 
 */
public enum CollectionType {
	array, arrayList, map, mapList, bean, beanList, column, columnList
}
